import 'package:flutter/material.dart';

class CampusMapViewStateful extends StatefulWidget {
  final State<CampusMapViewStateful> state;

  const CampusMapViewStateful({Key key, this.state}) : super(key: key);

  @override
  State<CampusMapViewStateful> createState() => state;
}
